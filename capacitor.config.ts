import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.gaskenn.photobooth',
    appName: 'Gaskenn Photobooth Android',
    webDir: 'dist',
    bundledWebRuntime: false,
};

export default config;
